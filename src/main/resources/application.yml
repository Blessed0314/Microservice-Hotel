server:
  port: 8090

spring:
  datasource:
    url: http://127.0.0.1:40463/reserva-hotel
    username: ${username}
    password: ${password}
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true